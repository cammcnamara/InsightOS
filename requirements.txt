# FastAPI server and Jinja2 templating
fastapi
uvicorn[standard]
jinja2

# Data handling and modeling
pandas
scikit-learn
numpy

# Causal Inference & SHAP (for future steps)
shap
causalml

# Optional (but recommended)
python-multipart  # Required for file uploads